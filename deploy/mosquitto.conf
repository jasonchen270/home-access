# Minimal Mosquitto config for local dev. DO NOT use anonymous auth in production;
# add `password_file` + ACLs there.
listener 1883
allow_anonymous true
persistence true
persistence_location /mosquitto/data/
